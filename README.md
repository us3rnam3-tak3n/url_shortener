# Url Shortener

Created a simple Url shortener, using Golang.

  - User can either post a link, and get its short link
  - Or upload a file containing multiple URLs, and their short links get updated in the database.
